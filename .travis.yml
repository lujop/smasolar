sudo: false
dist: trusty
language: java

matrix:
  include:
    - jdk: oraclejdk10
    - jdk: oraclejdk11

script:
  - echo PATH = ${PATH}
  - echo JAVA_HOME = ${JAVA_HOME}
  - java -version
  - ./gradlew --version
  - ./gradlew test